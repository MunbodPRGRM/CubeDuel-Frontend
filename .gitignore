# --- dependency ---
node_modules/

# --- build output ---
dist/
dist-ssr/
*.tsbuildinfo

# --- ตัวแปรสภาพแวดล้อม (ห้าม commit — .env.example ต้อง track ไว้) ---
.env
.env.local
.env.*.local
*.local

# --- log / ไฟล์ชั่วคราว ---
*.log
npm-debug.log*
coverage/

# --- Capacitor (เฟส 9) — โฟลเดอร์ที่ generate ใหม่ได้ ---
android/
ios/

# --- editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
